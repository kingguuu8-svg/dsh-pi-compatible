# Install both bundled presets into the user's DSH preset root.
# Existing files are not overwritten unless force: true is configured here.
- insert:
    - id: dsh-pi-compatible
      name: dsh-pi-compatible
